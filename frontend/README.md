# Task Manager Frontend

React 18 frontend for the local Simple Task Manager application.

## Run

```bash
npm install
npm start
```

The development server runs on `http://localhost:3000` and proxies API requests to the Spring Boot backend on port 8080.

## Test

```bash
npm test -- --watchAll=false
npm run build
```